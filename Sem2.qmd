---
title: "Semana 2"
format: html
editor: visual
---

### Asignacion 2 (Limpieza de datos)

Estas serian las librerias a utilizar.

```{r}
library(DT)
library(readxl)
```

A continuacion se procede a filtrar los datos que contienen espacios de celdad en blanco o en NA, como observaremos a continuacion, contienen un total de 5 celdad en blanco por columna.

```{r}
sapply(Archivo_Excel_Nuevo, function(x) sum(is.na(x)))
```

Luego de obtener los espacios de las celdas vacias e cada columna, se procede a la eliminacion de la misma, como podemos observas a continuacion.

```{r, message=FALSE}
eliminacion_Celda_vacia <- na.omit(Archivo_Excel_Nuevo)
head(eliminacion_Celda_vacia, n=78)
datatable(eliminacion_Celda_vacia)
```
